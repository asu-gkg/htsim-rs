# htsim-viz

Vue 3 + Vite frontend for visualization playback.

## Dev

```bash
cd tools/viz
npm install
npm run dev
```

Then open the URL from Vite (usually http://localhost:5173).

## Build

```bash
cd tools/viz
npm run build
```

Static assets are in `tools/viz/dist/`.
